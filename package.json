{
  "name": "vrcx-plugins",
  "version": "1.0.0",
  "description": "VRCX Plugins - TypeScript-based plugins for VRCX",
  "scripts": {
    "build": "node build.js",
    "build:dev": "node build.js --dev",
    "build:repo": "node build-repo.js",
    "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\""
  },
  "keywords": [
    "vrcx",
    "plugins",
    "vrchat"
  ],
  "author": "Bluscream",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.3.3"
  }
}
